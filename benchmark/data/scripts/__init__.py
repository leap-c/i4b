"""Scripts that build and finalize the benchmark corpus.

A package so they can be run as `python -m scripts.<name>` from `benchmark/`, and so the tests
can import the pieces worth testing directly.
"""
